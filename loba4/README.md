# Платформа «Лобачевский» - Форма подачи заявок

Система для подачи и отслеживания заявок граждан с интеграцией в базу данных.

## 🚀 Быстрый старт

### 1. Запуск сервера

Перейдите в папку `adminka` и запустите сервер:

```bash
cd adminka
npm install  # Если запускаете впервые
npm start
```

Сервер запустится на `http://localhost:3000`

### 2. Открытие формы

Откройте файл `index.html` в браузере или используйте:

```bash
# Если сервер запущен, форма доступна по адресу:
http://localhost:3000/loba4/index.html
```

## 📋 Функциональность

### Подача заявки (2 шага)

#### Шаг 1: Детали заявки (`index.html`)

- **Описание** - подробное описание проблемы (обязательное поле)
- **Адрес проблемы** - где находится проблема (обязательное поле)
- **Материалы** - загрузка файлов (до 4 файлов, необязательно)

#### Шаг 2: Личные данные (`step2.html`)

- **Фамилия, Имя, Отчество** (обязательные поля)
- **Телефон** - с автоматической маской +7 (XXX) XXX-XX-XX
- **Согласие** на обработку персональных данных

### Личный кабинет (`my-applications.html`)

- **Поиск по телефону** - просмотр всех заявок по номеру телефона
- **Две вкладки:**
  - **Активные** - заявки в статусах: "Новое", "Принято в работу", "На рассмотрении", "В работе"
  - **Закрытые** - остальные заявки
- **Карточки заявок** с информацией:
  - Дата подачи
  - Номер заявки
  - Источник (Сайт lbch.ru)
  - Текущий статус
- **Просмотр деталей** - клик по карточке открывает детальную страницу

### Детальный просмотр заявки (`complaint-detail.html`)

- **Хлебные крошки** - навигация "Мои заявки › Обращение №..."
- **Ответ на заявку** - официальный ответ от администрации (если есть)
- **Детали заявки** - полное описание проблемы
- **Адрес проблемы** - с визуализацией на карте
- **Временная шкала статусов:**
  - Отправлено (дата и время)
  - Принято в работу
  - В работе
  - Ответ дан (количество дней)
- **Информация об исполнителе** - кому назначена заявка

## 🗄️ Структура базы данных

Заявки сохраняются в таблицу `complaints` со следующими полями:

```sql
- id - уникальный номер заявки
- title - заголовок (адрес проблемы)
- description - описание проблемы
- author - ФИО заявителя
- author_phone - телефон заявителя
- author_address - адрес проблемы
- status - статус заявки (по умолчанию "Новое")
- external_system - источник (Сайт lbch.ru)
- created_date - дата создания
- created_at - timestamp создания
```

## 🔄 Интеграция с API

### Endpoints

**Создание заявки:**

```
POST /api/complaints
Content-Type: application/json

{
  "title": "Адрес проблемы",
  "description": "Описание",
  "author": "Иванов Иван Иванович",
  "author_phone": "+7 (904) 904-13-28",
  "author_address": "ул. Ленина, 1",
  "status": "Новое",
  "external_system": "Сайт lbch.ru"
}
```

**Получение заявок по телефону:**

```
GET /api/complaints-by-phone/:phone
```

**Получение заявки по ID:**

```
GET /api/complaints/:id
```

## 💾 Хранение данных

- **sessionStorage** - временные данные между шагами формы
- **localStorage** - сохранение номера телефона пользователя для личного кабинета
- **SQLite база данных** - постоянное хранение всех заявок (`adminka/complaints.db`)

## 📱 Адаптивный дизайн

Все страницы оптимизированы для мобильных устройств и планшетов.

## 🔧 Файлы проекта

```
loba4/
├── index.html              # Шаг 1: Детали заявки
├── step2.html              # Шаг 2: Личные данные
├── my-applications.html    # Личный кабинет
├── api-client.js           # API клиент для работы с сервером
└── README.md               # Эта инструкция

adminka/
├── server.js               # Express сервер
├── database.js             # Работа с SQLite
├── api.js                  # API клиент для админки
└── complaints.db           # База данных (создается автоматически)
```

## ⚠️ Требования

- Node.js (версия 14 или выше)
- Установленные npm пакеты в папке `adminka`:
  - express
  - sqlite3
  - cors
  - body-parser

## 🐛 Устранение неполадок

### Ошибка: "Сервер недоступен"

- Убедитесь, что сервер запущен: `cd adminka && npm start`
- Проверьте, что сервер работает на порту 3000

### Заявки не отображаются

- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что номер телефона совпадает с тем, который использовался при подаче заявки
- Телефон должен быть в формате: `+7 (XXX) XXX-XX-XX`

### База данных не создается

- Проверьте права доступа к папке `adminka`
- Убедитесь, что пакет `sqlite3` установлен: `npm install sqlite3`

## 📞 Контакты

Платформа «Лобачевский»  
Администрация Нижнего Новгорода  
Телефон: +7 (831) 467-10-01
