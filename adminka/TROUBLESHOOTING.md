# 🔧 Устранение проблемы "Данные не сохраняются"

## Проблема

Форма показывает "Данные сохранены", но в admin.html ничего нет.

## Решение

### Шаг 1: Остановите сервер

В терминале нажмите `Ctrl + C`

### Шаг 2: Удалите старую базу данных

```bash
# Windows PowerShell
del complaints.db

# Windows CMD
del complaints.db

# Linux/Mac
rm complaints.db
```

### Шаг 3: Тест базы данных

```bash
npm run test-db
```

Вы должны увидеть:

```
🧪 Тестирование базы данных...

✅ Подключено к базе данных SQLite
✅ Таблицы созданы/проверены
📝 Создаем тестовую заявку...
💾 Попытка сохранения в БД...
✅ Запись добавлена в БД, ID: 1
✅ Заявка создана, ID: 1

📊 Получаем все заявки...
✅ Найдено заявок: 1

🎉 Все тесты завершены!
```

### Шаг 4: Запустите сервер заново

```bash
npm start
```

### Шаг 5: Откройте админ-панель

```
http://localhost:3000/admin.html
```

Вы должны увидеть тестовую заявку!

### Шаг 6: Проверьте логи

В терминале сервера вы увидите логи при сохранении:

```
📝 Создание заявки: { title: '...', ... }
💾 Попытка сохранения в БД...
✅ Запись добавлена в БД, ID: 2
✅ Заявка создана, ID: 2
```

## Дополнительная диагностика

### Проверка 1: База данных существует?

```bash
dir complaints.db     # Windows
ls -la complaints.db  # Linux/Mac
```

Должен быть файл размером > 0 байт

### Проверка 2: Консоль браузера

Откройте F12 в браузере и посмотрите вкладку Console.
Не должно быть ошибок при сохранении.

### Проверка 3: Network вкладка

1. Откройте F12 → Network
2. Сохраните форму
3. Найдите запрос `/api/complaints`
4. Проверьте:
   - Status: 200 OK
   - Response: `{"message":"Заявка создана успешно","id":...}`

### Проверка 4: Терминал сервера

Смотрите логи в терминале при сохранении:

```
📝 Создание заявки: {...}
💾 Попытка сохранения в БД: {...}
✅ Запись добавлена в БД, ID: X
✅ Заявка создана, ID: X
```

Если видите `❌ Ошибка SQL:` - значит проблема в базе данных.

## Частые проблемы

### 1. "Cannot find module 'sqlite3'"

```bash
npm install
```

### 2. "SQLITE_CANTOPEN"

- Проблема с правами доступа
- Решение: Запустите терминал от имени администратора

### 3. База данных заблокирована

- Закройте DB Browser если открыт
- Перезапустите сервер

### 4. Порт занят

```
Error: listen EADDRINUSE: address already in use :::3000
```

Решение:

- Найдите процесс: `netstat -ano | findstr :3000`
- Убейте процесс: `taskkill /PID <номер> /F`

## Проверка через SQLite напрямую

```bash
sqlite3 complaints.db

# Посмотреть таблицы
.tables

# Посмотреть все заявки
SELECT * FROM complaints;

# Посмотреть количество
SELECT COUNT(*) FROM complaints;

# Выход
.quit
```

## Все еще не работает?

### Создайте минимальный тест:

```javascript
// Откройте консоль браузера (F12) и выполните:

API.createComplaint({
  title: "TEST",
  author: "TEST USER",
  status: "Новое",
})
  .then((result) => {
    console.log("Результат:", result);
    // Сразу проверяем
    return API.getAllComplaints();
  })
  .then((data) => {
    console.log("Всего заявок:", data.data.length);
    console.table(data.data);
  });
```

Если видите заявку в console.table - значит БД работает!
Просто обновите админ-панель (Ctrl+R).

## Файл базы данных

Путь к базе: `D:\test\adminka\complaints.db`

Откройте этот файл в DB Browser for SQLite чтобы точно увидеть что в базе.
