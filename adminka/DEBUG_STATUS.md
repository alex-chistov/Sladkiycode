# Проблемы с сохранением статуса

## Возможные причины:

1. **Статус не сохраняется в БД правильно**

   - Проверьте в консоли браузера логи: `📤 Новый статус:` должен совпадать с `📊 Статус из БД:`
   - Если не совпадает, проблема в функции `updateComplaint` в `database.js`

2. **При обновлении страницы (F5) статус сбрасывается**

   - Проверьте, что статус правильно загружается из БД в `loadComplaintData()`
   - Проверьте, что `lastSavedStatus` правильно устанавливается

3. **Опции не обновляются после сохранения**

   - Проверьте, что `updateActionOptions()` вызывается с правильным статусом
   - Проверьте логи: `🔄 updateActionOptions вызвана. Исходный статус:` должен быть правильным

4. **Проблема с маппингом действий**
   - Функция `mapActionToStatus()` должна правильно определять новый статус
   - Проверьте логи: `🧭 mapActionToStatus` должен показывать правильный `currentStatus` и `newStatus`

## Как проверить:

1. Откройте консоль браузера (F12)
2. Выполните действие (например, выберите "На модерации" и отправьте)
3. Проверьте логи:

   - `🧭 mapActionToStatus` - должен показать правильный `newStatus`
   - `📤 Новый статус:` - должен совпадать с `newStatus` из маппинга
   - `📊 Статус из БД:` - должен совпадать с `📤 Новый статус:`
   - `🔄 updateActionOptions вызвана. Исходный статус:` - должен быть правильным
   - `✅ Шаг X:` - должен соответствовать текущему этапу

4. Нажмите F5 и проверьте:
   - `📊 Статус заявки при загрузке:` - должен быть правильным
   - `🔄 updateActionOptions вызвана. Исходный статус:` - должен быть правильным
   - `✅ Шаг X:` - должен соответствовать текущему этапу

## Возможные исправления:

1. Увеличить задержку перед перезагрузкой данных (сейчас 300ms)
2. Добавить дополнительную проверку статуса из БД
3. Убедиться, что статус правильно нормализуется (убираются пробелы)
4. Проверить, что в БД статус сохраняется правильно (проверить `database.js`)
