# 📋 Система управления заявками (Adminka)

Веб-приложение для управления заявками граждан с базой данных SQLite.

## 🚀 Быстрый старт

### 1. Установка зависимостей

Откройте терминал (PowerShell или CMD) в папке проекта и выполните:

```bash
npm install
```

Эта команда установит все необходимые пакеты:

- **express** - веб-сервер
- **sqlite3** - база данных
- **body-parser** - обработка данных форм
- **cors** - поддержка кросс-доменных запросов

### 2. Запуск сервера

```bash
npm start
```

Или для режима разработки с автоперезагрузкой:

```bash
npm run dev
```

### 3. Откройте приложение

Перейдите в браузере по адресу:

```
http://localhost:3000
```

## 📁 Структура проекта

```
adminka/
├── index.html          # Главная страница
├── style.css           # Стили
├── script.js           # Клиентский JavaScript
├── api.js              # API клиент
├── server.js           # Сервер Express
├── database.js         # Работа с БД SQLite
├── package.json        # Зависимости
├── complaints.db       # База данных (создаётся автоматически)
└── README.md           # Эта инструкция
```

## 💾 База данных

### Таблицы:

#### 1. **complaints** - Заявки

- `id` - уникальный номер
- `title` - заголовок
- `description` - описание
- `author` - автор заявки
- `author_email` - email
- `author_phone` - телефон
- `status` - статус (Новое, В работе, Закрыто и т.д.)
- `assigned_to` - кому назначено
- `deadline` - срок исполнения
- И другие поля...

#### 2. **history** - История изменений

- `id` - ID записи
- `complaint_id` - ID заявки
- `change_date` - дата изменения
- `author` - кто изменил
- `field_name` - название поля
- `old_value` - старое значение
- `new_value` - новое значение

#### 3. **processing** - Обработка заявок

- Данные обработки заявок
- Оценки, комментарии, SMS

## 🔌 API Endpoints

### Заявки

- `GET /api/complaints` - Получить все заявки
- `GET /api/complaints/:id` - Получить заявку по ID
- `GET /api/complaints-by-phone/:phone` - Получить заявки по номеру телефона
- `POST /api/complaints` - Создать новую заявку
- `PUT /api/complaints/:id` - Обновить заявку
- `DELETE /api/complaints/:id` - Удалить заявку

### История

- `POST /api/history` - Добавить запись в историю
- `GET /api/history/:complaintId` - Получить историю заявки

### Интеграция с формой loba4

Форма подачи заявок граждан доступна по адресу:

- `http://localhost:3000/loba4/index.html` - Подача заявки (шаг 1)
- `http://localhost:3000/loba4/step2.html` - Личные данные (шаг 2)
- `http://localhost:3000/loba4/my-applications.html` - Личный кабинет

### Открытие заявок по ID

Теперь можно открывать заявки напрямую по ссылке:

```
http://localhost:3000/adminka/index.html?id=123
```

При открытии:

- ✅ Автоматически загружаются данные из БД
- ✅ Все поля формы заполняются
- ✅ Загружается история изменений
- ✅ Можно редактировать и сохранять изменения

### Список заявок

Просмотр всех заявок в удобной таблице:

```
http://localhost:3000/adminka/list.html
```

Подробнее см. `INTEGRATION_WITH_LOBA4.md`

## ✨ Возможности

### Вкладка "Подробно"

- ✏️ Редактирование полей заявки
- 💾 Сохранение в базу данных
- 📁 Кнопки управления

### Вкладка "Обработка"

- 📝 Форма обработки заявки
- ⭐ Оценка качества
- 📤 Отправка SMS
- 📎 Прикрепление файлов
- ✅ Валидация обязательных полей

### Вкладка "История изменений"

- 📊 Таблица с историей
- 🎨 Цветные бейджи статусов
- 📅 Временные метки

## 🛠 Как использовать

### Создание новой заявки:

```javascript
// В консоли браузера или в коде
const newComplaint = {
  title: "Яма на дороге",
  description: "Большая яма на улице Ленина",
  author: "Иван Иванов",
  author_phone: "+79001234567",
  status: "Новое",
};

API.createComplaint(newComplaint)
  .then((result) => console.log("Создано:", result))
  .catch((error) => console.error("Ошибка:", error));
```

### Обновление заявки:

```javascript
API.updateComplaint(1, {
  status: "В работе",
  assigned_to: "Аналитический центр города",
}).then((result) => console.log("Обновлено:", result));
```

### Получение всех заявок:

```javascript
API.getAllComplaints().then((data) => console.log("Все заявки:", data));
```

## 🔧 Настройка

### Изменить порт сервера:

Откройте `server.js` и измените:

```javascript
const PORT = 3000; // Ваш порт
```

### Изменить путь к БД:

Откройте `database.js` и измените:

```javascript
const dbPath = path.join(__dirname, "complaints.db");
```

## 📝 Примеры использования

### Пример 1: Сохранение заявки через форму

1. Откройте вкладку "Подробно"
2. Нажмите "Редактировать"
3. Заполните поля
4. Нажмите "Сохранить"
5. Данные автоматически сохранятся в БД

### Пример 2: Обработка заявки

1. Откройте вкладку "Обработка"
2. Выберите действие
3. Заполните обязательные поля (отмечены \*)
4. Нажмите "Отправить"
5. Данные сохранятся и добавятся в историю

## ⚠️ Устранение неполадок

### Ошибка: "Cannot find module"

```bash
npm install
```

### Ошибка: "Port already in use"

Измените порт в `server.js` или закройте программу, использующую порт 3000.

### База данных не создаётся

Убедитесь, что у вас есть права на запись в папку проекта.

## 📊 Просмотр базы данных

Используйте любой SQLite клиент:

- [DB Browser for SQLite](https://sqlitebrowser.org/) (рекомендуется)
- [SQLite Studio](https://sqlitestudio.pl/)
- Или через командную строку: `sqlite3 complaints.db`

## 🎯 Следующие шаги

- [ ] Добавить аутентификацию пользователей
- [ ] Реализовать загрузку файлов
- [ ] Добавить поиск и фильтрацию заявок
- [ ] Экспорт данных в Excel
- [ ] Email уведомления
- [ ] Статистика и отчёты

## 📞 Поддержка

При возникновении проблем проверьте:

1. Запущен ли сервер (`npm start`)
2. Открыт ли правильный URL (`http://localhost:3000`)
3. Установлены ли зависимости (`npm install`)
4. Консоль браузера на наличие ошибок (F12)

## 📄 Лицензия

ISC

---

**Версия:** 1.0.0  
**Дата:** 2025-11-21
